import { Insets } from "react-native";

// Minimum recommended touch target padding for small icon-only buttons
export const HIT_SLOP_8: Insets = { top: 8, right: 8, bottom: 8, left: 8 };
export const HIT_SLOP_12: Insets = { top: 12, right: 12, bottom: 12, left: 12 };
